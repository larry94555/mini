# imini workspace

This directory is bind-mounted into the imini container at `/workspace` and is the agent's working
directory (`agent.workspace-root`). Drop the project files you want imini to read, edit, search, and
run commands against here. Edits the agent makes show up on your host in this folder.
