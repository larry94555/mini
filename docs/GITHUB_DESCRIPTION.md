# Suggested GitHub Repository Description

GitHub repository descriptions are repository metadata, not files in the git tree. Update it manually in the GitHub UI:

1. Open the repository page.
2. Click the gear/settings icon next to the repository description.
3. Replace the current description.

Suggested description:

```text
A local, llama.cpp-backed Java/Spring learning harness that demonstrates Claude Code-style agent loops, tools, permissions, sessions, retrieval, and safety boundaries.
```

This also fixes the typo from `mamner` to `manner` if that typo is still present in the repository description.
